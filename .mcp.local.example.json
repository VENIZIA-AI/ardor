{
  "$comment": "Optional. `.mcp.json` runs the published atlas; point it at a local IGNIS checkout instead when you are changing the server itself. Copy to .mcp.local.json (gitignored) or edit .mcp.json locally.",
  "mcpServers": {
    "ardor-atlas": {
      "command": "bun",
      "args": [
        "../ignis/packages/atlas/src/cli.ts",
        "mcp",
        "--root",
        "."
      ]
    }
  }
}
